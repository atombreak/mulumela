import { SignIn } from '../../../components/auth';

export default function SignInPage() {
  return <SignIn />;
}

export const metadata = {
  title: 'Sign In - Mulumela',
  description: 'Sign in to your Mulumela account',
}; 